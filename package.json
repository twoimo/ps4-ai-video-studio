{
  "name": "ps4-ai-video-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "analyze": "bun scripts/analyze-channel.mjs",
    "channel:refresh": "bun scripts/refresh-channel.mjs",
    "benchmark:metadata": "bun scripts/refresh-benchmark.mjs",
    "benchmark:refresh": "bun run channel:refresh && bun run benchmark:metadata && bun run analyze && bun run quality:rlm",
    "yt-dlp:update": "bun scripts/update-yt-dlp.mjs",
    "quality:rlm": "bun scripts/run-rlm-analysis.mjs",
    "benchmark:media": "bun scripts/benchmark-media.mjs",
    "monitor:gemini": "bun scripts/monitor-gemini-production.mjs",
    "gemini:abandon-legacy": "bun scripts/abandon-legacy-gemini-submission.mjs",
    "dev": "bun --watch src/server.mjs",
    "start": "bun src/server.mjs",
    "check:syntax": "for file in src/*.mjs scripts/*.mjs public/*.js test/*.mjs tests/*.mjs; do node --check \"$file\"; done",
    "test": "bun test",
    "check": "bun run check:syntax && bun test && git diff --check"
  }
}
